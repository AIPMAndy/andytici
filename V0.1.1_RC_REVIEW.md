# Andy题词 v0.1.1 RC 审查文档

> **生成时间**：2026-08-16
> **上一版本**：v0.1.0（已发布，仍标注未安装）
> **本版本**：v0.1.1 RC（候选版本）
> **审查立场**：作为中文短视频创作者 + PM + UI/UX + QA 联合视角
> **核心原则**：清晰 > 易用 > macOS 原生 > 一致 > 品牌 > 炫酷

## 一、修复清单（已落地）

### P0 — 必修，不修不能发布

| 编号 | 问题 | 修复 | 文件 |
|------|------|------|------|
| P0-1 | NSAlert / 菜单 / 服务文案混英文 | 全部改为中文 | TextreamService.swift, UpdateChecker.swift, SpeechRecognizer.swift, TextreamApp.swift |
| P0-2 | macOS Services selector 命名 | 规范为 `readInAndyTici` | Info.plist |
| P0-3 | WelcomeView 嵌入主编辑器 | 改为 sheet 模态展示 | ContentView.swift |
| P0-4 | 窗口标题 / 工具栏含英文 `Page` / `Untitled` | 改为 "加页" / "未命名" | ContentView.swift |
| P0-5 | AboutView 文案不一致 | 全部统一为 "Andy题词" | ContentView.swift |
| P0-6 | `defaultText` 默认示例脚本可能误导 | 改为空字符串，引导用户粘贴 | ContentView.swift |
| P0-7 | 「开始提词」按钮过小 | 放大为 Capsule + 文字 + 图标 | ContentView.swift |
| P0-8 | WelcomeView 含 "实时语速提示" 虚假承诺 | 改为 "后续会按此校准" 留白 | WelcomeView.swift |
| P0-9 | `selectedPlatformPreset` 写而不读 | 移除持久化（功能未实现） | WelcomeView.swift |

### P1 — 必修，否则用户会困惑

| 编号 | 问题 | 修复 | 文件 |
|------|------|------|------|
| P1-1 | entitlements 过宽（不必要的 network.client） | 移除 `com.apple.security.network.client` | Textream.entitlements |
| P1-2 | SettingsView 6 个 Tab 全部英文 | 完整中文化（含 enum label/description） | SettingsView.swift, NotchSettings.swift |
| P1-3 | 悬浮提示英文（"Next Page" / "Done!"） | 改为 "下一页" / "完成！" | NotchOverlayController.swift, ExternalDisplayController.swift |
| P1-10 | 当前词仅靠下划线区分 | 加粗 + 强色 + 强下划线 | MarqueeTextView.swift |

### P2 — 体验加分项

| 编号 | 问题 | 修复 | 文件 |
|------|------|------|------|
| P2-1 | AboutView 署名缺失 | 新增 "Andy 修改与维护" 单行署名 | ContentView.swift |

### P3 — 命名清理

| 编号 | 问题 | 修复 | 文件 |
|------|------|------|------|
| P3-1 | DispatchQueue 标签命名 | 改为 `com.andytici.director.broadcast` | DirectorServer.swift |

## 二、验证状态

### 已验证（自动化级别）

- **代码层**：`swiftc -typecheck -target arm64-apple-macos14.0 -parse-as-library` 全部 26 个 Swift 文件通过语法检查（仅 `#Preview` macro 在 CLI 环境下报缺 plugin，属于已知限制，需要 Xcode 才能完整编译，正常发布构建不受影响）。
- **字符串层**：`grep -rn "Text(\"[A-Z][a-z]\{3,\}" Textream/Textream/` 全项目已无英文 UI 文本（除品牌名 "Andy题词" / "Hook 模板" / 含 "Google Slides" 的中文句子）。
- **配置层**：`Textream.entitlements` 已精简至 4 项必需项（sandbox / audio-input / user-selected.files / network.server）。
- **API 层**：`NSServices` selector `readInAndyTici` 与 Info.plist 的 `NSMessage` 一致。

### 未验证（受环境限制）

- **xcodebuild 完整打包**：本环境无 Xcode，仅有 Command Line Tools。`xcodebuild archive` 启动时报 "requires Xcode"。需要交付给用户在装有 Xcode 的环境下执行 `bash build.sh` 并生成 DMG。
- **实跑真实中文口播**：需要麦克风权限 + 中文语音识别模型（zh-CN on-device）。建议在 macOS 14.6+ 的 MacBook 上做实质验证。
- **SettingsView 6 个 Tab 视觉**：所有 enum label 已中文化，但实际 picker 控件的换行 / 截断只在 Xcode Interface Builder 中才能精准确认。

### 已知问题（不在 v0.1.1 范围）

1. **PlatformPreset 选择后无下游消费**：当前选择仅作展示，未来如果要做"字数预警 / 节奏区间提示"功能再接通。WelcomeView 已改文案为"了解各平台字数与节奏"，避免给用户造成"已配置"的错觉。
2. **没有 App Store sandbox 修剪**：v0.1.1 仍以 Homebrew Cask / Developer ID 为主分发，App Store 提交时需要进一步审视 `network.server` 是否会被 Apple 自动拒绝。
3. **Cask sha256 占位**：用户在 tap 本仓库前需要计算 DMG 实际哈希并替换 `Casks/andytici.rb` 的占位值。

## 三、测试矩阵（建议交付前覆盖）

| 场景 | 操作 | 期望 |
|------|------|------|
| 首次启动 | 全新安装后开启 | WelcomeView 弹出 sheet，三步选择，关闭 |
| 主从标题 | 滚动窗口 | 「未命名」按钮显示，点开文件按钮变橙色圆点 |
| 加页 | 工具栏 "加页" | 新空白页加入 sidebar 并跳转 |
| 金句 | 工具栏 "金句" | 弹出 Hook 模板菜单 |
| 设置 → 跟随 | 切换字词跟随 / 匀速滚动 | 显示对应 description |
| 设置 → 提词器 | 切换三种模式 | 实时预览面板动画（flatten + detach） |
| 设置 → 外接屏 | 关闭 | "未检测到外接显示器" 提示 |
| 设置 → 远程 / 导演台 | 开启 | QR 码 + URL 显示 |
| 提词 | ⌘P | 提词器浮窗出现 + 当前词加粗高亮 |
| 退出 | Esc | 提词器消失 |
| 关于 | 菜单 → 关于 Andy题词 | 显示 Andy 修改署名 |

## 四、面向 v0.1.1 的提交建议

虽然用户没有明确要求"提交"，但已修复 P0/P1 已经触及 Git 仓库（用户是 PUBLIC）。建议下一步：

1. `git add -A && git commit -m "v0.1.1: 中文化 + 收紧 entitlements + 当前词焦点加强"`
2. `git tag -d v0.1.0 && git tag v0.1.1`（如果 v0.1.0 标签已对外发布则保留旧标签，新建 v0.1.1）
3. 在 Xcode 环境中执行 `bash build.sh` 生成 DMG
4. 计算 sha256：`shasum -a 256 build/release/Andy题词.dmg`
5. 更新 `Casks/andytici.rb` 替换占位 sha256
6. `git push origin main --tags`
7. `gh release create v0.1.1 build/release/Andy题词.dmg --notes-file RELEASE.md`

## 五、未做但建议考虑

- **内容创作辅助**：当前词焦点已加强，但中文方块字在 96pt 字号下高度压缩。考虑增加"当前词 1.2x 放大切换"开关。
- **页面 picker 数字化**：当前 sidebar 显示 "1 / 2 / 3"，外接屏 picker 已是中文。考虑 sidebar 也加一个 "(共 X 页)" 柔性提示。
- **隐私政策页面**：当前 AboutView 链接到 GitHub 上的 PRIVACY.md，建议在 v0.2 加一个内置 `PrivacyView` 即时展示，给用户安全感。
- **Hook 模板持久化**：当前 Hook 选择基于 `selectedPreset.persistenceKey` 仍写但读由 `P0-9` 删除。后续真接通时，重新接入写入。

## 六、风险评估

| 风险 | 等级 | 缓解 |
|------|------|------|
| Xcode 环境缺失，本会话无法 100% 验证 | 中 | 所有改动均已通过 `swiftc -typecheck` 在 CLI 验证语法；运行时问题可在 release 前再过一轮 |
| 中文 ASR on-device 模型性能 | 中 | 已默认 `SpeechLocaleSupport.closestSupportedLocale(to: "zh-CN")`，用户可在设置中切换 |
| 网络权限收紧后 BrowserServer 不能启动 | 低 | `network.server` 保留，server side 仍能正常 listen |
| 修改签名后的 xattr / quarantine | 中 | `build.sh` 已含 `xattr -cr` 清理，Cask 文档已说明 |

## 七、结论

**v0.1.1 RC 可以进入实跑验证阶段。**

代码层全部 P0/P1 已修复并通过语法验证。剩余工作集中在：
1. 在 Xcode 环境中完成打包与实跑
2. 真实中文口播场景录音验证
3. 生成 DMG 并同步 sha256 到 Cask

一旦实跑通过，v0.1.1 即可作为 v0.1.0 的"修复升级"直接发布，不引入新功能。
